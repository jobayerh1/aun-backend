--- stock/public/js/purchase.js
+++ live/public/js/purchase.js
@@ -600,6 +600,7 @@
             { data: 'transaction_date', name: 'transaction_date' },
             { data: 'ref_no', name: 'ref_no' },
             { data: 'location_name', name: 'BS.name' },
+            { data: 'business_location_id', name: 'BS.location_id', defaultContent: '' },
             { data: 'name', name: 'contacts.name' },
             { data: 'status', name: 'status' },
             { data: 'payment_status', name: 'payment_status' },
@@ -639,8 +640,10 @@
             $('.footer_payment_status_count').html(__count_status(data, 'payment_status'));
         },
         createdRow: function(row, data, dataIndex) {
+            //CUSTOM: index shifted 5 -> 6 after the Location ID column was
+            //inserted; this must stay on the Purchase Status cell.
             $(row)
-                .find('td:eq(5)')
+                .find('td:eq(6)')
                 .attr('class', 'clickable_td');
         },
     });
