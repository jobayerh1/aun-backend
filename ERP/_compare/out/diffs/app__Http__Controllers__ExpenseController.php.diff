--- stock/app/Http/Controllers/ExpenseController.php
+++ live/app/Http/Controllers/ExpenseController.php
@@ -88,6 +88,8 @@
                             'transactions.recur_repetitions',
                             'transactions.subscription_repeat_on',
                             'bl.name as location_name',
+                            //CUSTOM: human-readable Location ID (e.g. "AUN") for its own column
+                            'bl.location_id as business_location_id',
                             DB::raw("CONCAT(COALESCE(U.surname, ''),' ',COALESCE(U.first_name, ''),' ',COALESCE(U.last_name,'')) as expense_for"),
                             DB::raw("CONCAT(tr.name ,' (', tr.amount ,' )') as tax"),
                             DB::raw('SUM(TP.amount) as amount_paid'),
