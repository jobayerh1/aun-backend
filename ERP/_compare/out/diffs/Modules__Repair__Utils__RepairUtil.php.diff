--- stock/Modules/Repair/Utils/RepairUtil.php
+++ live/Modules/Repair/Utils/RepairUtil.php
@@ -168,7 +168,7 @@
         }
     }
 
-    public function sendJobSheetUpdateSmsNotification($sms_body, $job_sheet)
+public function sendJobSheetUpdateSmsNotification($sms_body, $job_sheet)
     {
         $business_id = $job_sheet->business_id;
         $customer = $job_sheet->customer;
@@ -185,7 +185,9 @@
         //Send sms
         if (! empty($data['sms_settings']) && ! empty($customer->mobile) && ! empty($data['sms_body'])) {
             $response = $this->sendSms($data);
-            if (! empty($response) && $response->getStatusCode() == 200) {
+            
+            // ✅ CHANGED: Safely handle both the new background queue 'true' response and old API responses
+            if ($response === true || (is_object($response) && method_exists($response, 'getStatusCode') && $response->getStatusCode() == 200)) {
                 $is_sent = __('repair::lang.sms_sent');
             } else {
                 $is_sent = __('repair::lang.sms_not_sent');
